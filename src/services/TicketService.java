package services;

import dtos.GenerateTicketRequestDto;
import models.Ticket;
import models.VehicleType;

public class TicketService {

    public Ticket generateTicket(int gateNumber, String vehicleNumber, VehicleType vehicleType) {
    }
}
